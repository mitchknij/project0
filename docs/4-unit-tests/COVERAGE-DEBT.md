# Coverage Debt Ledger

Format: `path | why hard | escape plan`

- `Assets/Scripts/Managers/Content/ContentValidator.cs:71-76` (tile-targeting kind ⇔ TilePattern presence pairing checks) | The checks run inline in `Validate()` over the static `ClassesRepo.All` registry — no injectable seam; testing the reject paths would require mutating production static content (2026-07-17, v0.5.0 tile targeting). `ValidateTilePattern` internals ARE covered (via reflection in ContentValidationTests); only the pairing rules lack a reject-path test — the accept path is exercised implicitly because shipped content passes validation with tile skills present. | Extract skill validation into a `ValidateSkill(ClassSkillDef, List<string>)` helper taking the def directly, then test reject paths without touching the registry.
- `Assets/Scripts/Managers/GameManager.cs` (AssignSkillToBar / SwapSkillBarSlots / ClearSkillBarSlot) | GameManager is a MonoBehaviour singleton (Awake/DontDestroyOnLoad, state reachable only via CreateFamily/CreateCharacter/SelectCharacter); no EditMode test instantiates it anywhere — the codebase deliberately keeps MonoBehaviour lifecycle in PlayMode. Harness setup would dwarf the assertions (mock-pain tripwire, 2026-07-16, v0.4.0 skillbar). | Persistence behavior (seeding, normalization) is covered behaviorally in SaveCompatibilityTests; guard/list logic covered via StateAndValidationTests. Cover the mutation API in GameplayLoopSmokeTests (PlayMode) or extract the bar-mutation logic into a testable Core helper if it grows.
